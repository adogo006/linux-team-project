## 프로젝트 개요

이 프로젝트는 웹서비스 기반 팀 프로젝트 협업툴을 만드는 프로젝트입니다.

사용자가 프로젝트를 만들면 DB에 해당 프로젝트가 생성되고 하위 파일 노드들이 생성됩니다.
이 중 `node_type`이 `file`이면 `/user_uploads` 볼륨 공간에 파일 노드의 `uid`를 파일명으로 사용하는 실제 파일이 생성되고,
해당 경로가 파일 노드에 저장되는 구조를 목표로 합니다.

즉, 디렉토리 계층 구조는 DB에서 관리하고 파일들은 평면적으로 저장되는 구조입니다.

---

## 핵심 기능 및 현재 상태

- 회원가입 기능(닉네임 설정, 중복 X): 부분 완료
	- 회원가입/중복체크 API는 존재
	- 회원가입 시 중복 체크를 강제하지 않고 있어 DB 제약 예외 처리 보강 필요
- 로그인 기능: 부분 완료
	- 로그인/토큰 발급/갱신/로그아웃 API 존재
	- 비밀번호 해시 검증 미구현(평문 비교), `python-jose` 의존성 설치 필요
- 프로젝트 초대 및 생성, 제거 기능: 부분 완료
	- 생성은 구현
	- 초대/제거 엔드포인트는 미구현(관련 CRUD는 일부 존재)
- 프로젝트 목록 가져오는 기능: 완료(기본)
	- 참여 프로젝트 목록 조회 가능
	- 초대 유저 목록 닉네임 반환 로직 포함
- 프로젝트 열면 작업중인 인원(닉네임) 표현 기능: 미구현
	- 현재 `editing_users`는 빈 배열 고정 반환
- 프로젝트의 폴더 구조 및 파일 내용 가져오는 기능: 부분 완료
	- 파일 트리 조회는 가능
	- 개별 파일 내용 조회 API는 미구현
- 내용 수정중인 파일은 수정중 문구 + 닉네임 표시: 미구현
- 수정 완료 시 파일 히스토리 저장(닉네임, 시작/종료시간, 메시지): 부분 완료
	- 로그 저장/조회 구조는 있음
	- 파일 수정 흐름과 로그 연계 API가 아직 부족

---

## 개선 필요 사항 (우선순위)

1. 인증/보안
	 - `python-jose` 설치 및 런타임 검증
	 - 비밀번호 해시 저장/검증(`passlib[bcrypt]` 등)으로 변경
	 - 토큰 블랙리스트를 메모리(set)에서 Redis/DB 기반으로 전환
2. 기능 완성도
	 - 프로젝트 초대/제거 API 추가
	 - 파일 열기/저장 API 추가 및 `/user_uploads` 실제 파일 IO 연결
	 - 편집중 상태(락) 관리 API 추가
3. 데이터 정합성
	 - 회원가입 중복 처리 시 명시적 에러 응답 통일
	 - CRUD/응답 스키마 네이밍 일관성(`id`, `uid`, `nickname`) 정리
4. 운영 품질
	 - 주요 API 테스트 코드(회원가입/로그인/프로젝트/파일) 작성
	 - 예외 응답 포맷 표준화 및 로깅 강화

---

## 다음 구현 권장 순서

1. 인증 안정화(해시/토큰 의존성/예외 표준화)
2. 파일 열기/저장 + 히스토리 저장 연계
3. 편집중 상태 표시 기능
4. 프로젝트 초대/제거 API 마무리
